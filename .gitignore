# dependencies
node_modules/

# build output
dist/
dist-single/
*.tsbuildinfo

# local tooling (Claude Code launch config with machine-specific paths)
.claude/

# editor / OS noise
.DS_Store
Thumbs.db
*.local
*.log

# env (none used yet, but never commit secrets)
.env
.env.*
!.env.example

# vite dev-server temp config bundles
vite.config.ts.timestamp-*
